
<h3>Annual Volume Properties put on Sale for <%= @county %></h3>

<table class="table-borders">
  <thead>
    <tr>
      <th>Area</th>
      <th>Total Residents/Sites on Sale</th>
      <th>Detail</th>
    </tr>
  </thead>
  <tbody>
  <% @vhistvol.each do |itmvol|   %>
    <tr>
      <td> <%= itmvol['searchparam'] %></td>
      <td> <%= itmvol['totalval'] %></td>
      <td><%=  link_to "Detail", detailvol_path(itmvol['searchparam']) %></td>
    </tr>
 <% end %>
  </tbody>
</table>
<%= will_paginate @vhistvol %>
